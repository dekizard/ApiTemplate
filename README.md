# Create API template with clean architecture

This project helps to quickly create specific API-s for your organization.

- Install template

  dotnet new --install C:\..\ApiTemplate


- Create API

  dotnet new api -n Payment
